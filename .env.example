PORT=4000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# For production, swap better-sqlite3 (db/database.js) for MySQL/PostgreSQL
# per the Technology Stack in the proposal (MySQL / PostgreSQL).
DB_FILE=./db/barangay.sqlite
